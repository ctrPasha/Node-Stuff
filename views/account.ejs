<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Account</title>
  <link rel="stylesheet" href="/account.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css" rel="stylesheet">
</head>

<body>
  <div class="container-fluid" id="mainContainer">
    <div id="account">
      <h1>Account Settings</h1>
    </div>

    <div class="alert-container">
      <% if (alert) { %>
        <div class="alert alert-<%= alert === 'success' ? 'success' : 'danger' %> alert-dismissible fade show" role="alert">
          <%= alert === 'exists' ? 'Username already exists.' : alert === 'success' ? 'Successfully updated account information' : 'An error occurred changing account information' %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% } %>
    </div>
    
    
    
    <nav class="navbar navbar-light bg-dark mt-4">
      <div class="d-flex">
        <% if (user) { %>
          <a class="btn rounded-pill btn-primary mx-2" id="homeBtn" href="/">
            <i class="bi bi-house"></i> Home
          </a>
          <form action="/logout" method="POST" class="d-inline">
            <button type="submit" class="btn rounded-pill btn-primary" id="logoutBtn">
              <i class="bi bi-box-arrow-right"></i> Logout
            </button>
          </form>
          <a class="btn rounded-pill btn-primary mx-4" id="accountBtn" href="/account">
            <i class="bi bi-person"></i> Account
          </a>
        <% } %>
      </div>
    </nav>
    
    <div class="container-md" id="editContainer">
      <form action="/account?_method=PUT" method="POST">
        <h5 class="text-center">Edit Profile</h5>

        <label for="updateUser" class="form-label" id="newText">New Username</label>
        <input type="text" class="form-control" id="updateUser" placeholder="Enter New Username" name="userName">

        <label for="updatePass" class="form-label mt-4" id="passText">New Password</label>
        <input type="password" class="form-control" id="updatePass" name="password" placeholder="*********">
        <button type="submit" class="btn rounded-pill btn-primary" id="submit">Update</button>
      </form>
    </div>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
