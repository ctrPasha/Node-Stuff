<!DOCTYPE html>
<html lang="en">

<head>
  <link rel="stylesheet" href="/login.css">
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css" rel="stylesheet">
  <title>Login</title>
</head>

<body>
  <div class="container-fluid" id="mainContainer">

    <div id="login">
      <h1>Sign In</h1>
    </div>

    <nav class="navbar navbar-light bg-dark mt-4">
      <div class="d-flex">
        <a class="btn rounded-pill btn-primary mx-2" id="homeBtn" href="http://localhost:3000/">
          <i class="bi bi-house"></i> Home
        </a>

        <a class="btn rounded-pill btn-primary" id="loginBtn" href="http://localhost:3000/login">
          <i class="bi bi-box-arrow-in-right"></i> Login
        </a>

        <a class="btn rounded-pill btn-primary mx-4" id="signUp" href="http://localhost:3000/register">
          <i class="bi bi-person-plus"></i> Sign Up
        </a>
      </div>
    </nav>

    <!-- Alert container -->
    <div class="alert-container">
      <% if (alert) { %>
        <div class="alert alert-<%= alert === 'success' ? 'success' : 'danger' %> alert-dismissible fade show" role="alert">
          <%= alert === 'invalid' ? 'Invalid email or password.' : alert === 'success' ? 'Login successful! Welcome back.' : 'An error occurred during login.' %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% } %>
    </div>

    <div class="container-md" id="login-container">
      <form method="post" action="http://localhost:3000/login">
        <h3 class="text-center">Sign In</h3>
        <label for="inputEmail" class="form-label">Email Address</label>
        <input type="email" class="form-control" id="inputEmail" name="email" required>
        <label for="inputPass" class="form-label mt-3">Password</label>
        <input type="password" class="form-control" id="inputPass" name="password" required>
        <button type="submit" class="btn btn-primary mt-3" id="submit">Log In</button>
      </form>
    </div>

    <p class="mt-3">New User? <a href="http://localhost:3000/register" id="signUpText">Sign Up</a></p>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
